# Cloudflare Tunnel Configuration

# Option 1: Using Tunnel Token (Recommended)
# Get this from: https://one.dash.cloudflare.com/ -> Access -> Tunnels
TUNNEL_TOKEN=your-tunnel-token-here

# Option 2: Using API Token and Config File
CF_API_TOKEN=your-api-token-here
CF_ACCOUNT_ID=your-account-id-here
TUNNEL_NAME=my-tunnel

# Tunnel Configuration
TUNNEL_HOSTNAME=example.com
TUNNEL_URL=http://localhost:8080

# Multiple services example:
# Service 1: app.example.com -> http://app:3000
# Service 2: api.example.com -> http://api:8000
# Configure in config/config.yml